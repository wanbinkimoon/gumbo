import styled from 'styled-components';

export const Docs = styled.section.attrs({
  className: 'storybook-exclude',
})`
  -webkit-font-smoothing: antialiased;
  padding: 20px 40px 40px;
  background-color: rgb(255, 255, 255);
  margin: 20px 0;
`;
